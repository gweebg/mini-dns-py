# Database configuration file for the reverse.maki domain (local testing).

@ DEFAULT 0.127-inaddr.reverse.maki.

TTL DEFAULT 60

@ SOASP sp.0.127-inaddr.reverse.maki. TTL
@ SOAADMIN dns.admin@0.127-inaddr.reverse.maki TTL
@ SOASERIAL 105002 TTL

@ SOAREFRESH 45 TTL
@ SOARETRY 60 TTL
@ SOAEXPIRE 60 TTL

0.127-inaddr.reverse.maki. NS sp.0.127-inaddr.reverse.maki. TTL

sp.0.127-inaddr.reverse.maki. A 127.0.0.1:20026 TTL

127.0.0.1:20023 PTR maki. TTL
127.0.0.1:20020 PTR shijo.maki. TTL
127.0.0.1:20024 PTR iino.shijo.maki. TTL
127.0.0.1:20025 PTR reverse.maki. TTL
