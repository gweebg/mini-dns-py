# Database configuration file for the root domain (local testing).

@ DEFAULT .maki.

TTL DEFAULT 60

@ SOASP sp.maki. TTL
@ SOAADMIN dns.admin@maki TTL
@ SOASERIAL 105002 TTL

@ SOAREFRESH 45 TTL
@ SOARETRY 60 TTL
@ SOAEXPIRE 60 TTL

maki. NS sp.maki. TTL
shijo.maki. NS sp.shijo.maki. TTL
reverse.maki. NS sp.reverse.maki. TTL

sp.maki. A 127.0.0.1:20023 TTL
sp.shijo.maki. A 127.0.0.1:20020 TTL
sp.reverse.maki. A 127.0.0.1:20025 TTL


