# Database configuration file for the root domain (local testing).

@ DEFAULT shijo.maki.

TTL DEFAULT 60

@ SOASP sp.shijo.maki. TTL
@ SOAADMIN dns.admin@shijo.maki TTL
@ SOASERIAL 105012 TTL

@ SOAREFRESH 45 TTL
@ SOARETRY 60 TTL
@ SOAEXPIRE 60 TTL

shijo.maki. NS sp.shijo.maki. TTL
iino.shijo.maki. NS sp.iino.shijo.maki. TTL

shijo.maki. MX mx1.shijo.maki. TTL 10
shijo.maki. MX mx2.shijo.maki. TTL 20

sp.shijo.maki. A 127.0.0.1:20020 TTL
sp.iino.shijo.maki. A 127.0.0.1:20024 TTL

mx1.shijo.maki. A 10.0.0.3 TTL
mx2.shijo.maki. A 10.0.0.4 TTL
