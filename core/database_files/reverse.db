# Database configuration file for the top level domain 'reverse.maki.' #
# Used on reverse name resolution. #

# Default domain prefix set to 'reverse.maki.' #
@ DEFAULT reverse.maki.

# Default time to live set to one minute (60s) #
TTL DEFAULT 120

@ SOASP sp.reverse.maki. TTL
@ SOAADMIN dns.admin@reverse.maki TTL
@ SOASERIAL 105007 TTL

@ SOAREFRESH 60 TTL 
@ SOARETRY 30 TTL
@ SOAEXPIRE 60 TTL

10.0.4.10 PTR lycoris.maki. TTL
10.0.9.12 PTR lili.lycoris.maki. TTL

10.0.5.10 PTR recoil.maki. TTL
10.0.4.11 PTR bell.recoil.maki. TTL


